[ui]
default-command = "log"

[user]
name = "Siva Mahadevan"
email = "me@svmhdvn.name"

[git]
push = "origin"
fetch = "upstream"
colocate = false

[revset-aliases]
my_unbookmarked = "children(main) ~ ancestors(bookmarks()) ~ empty()"
